// A tuple is a typed array with a predefined length and types for each index.
// Tuples are great because they allow each element in the array to be a known typre of value.
var myTuple;
myTuple = [8, false, "Coding"];
myTuple.push('Something new and wrong');
console.log("MyTuple", myTuple);
